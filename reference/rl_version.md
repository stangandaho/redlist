# IUCN Red List and API version

Print the current version of the IUCN Red List of Threatened Species and
API

## Usage

``` r
rl_version()
```

## Value

Invisibly returns `NULL` after printing the Red List and API versions.

## Examples

``` r
if (FALSE) { # \dontrun{
rl_version()
} # }
```
