% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rl_eoo.R
\name{rl_eoo}
\alias{rl_eoo}
\title{Extent of occurrence (EOO) for IUCN criterion B}
\usage{
rl_eoo(x, coords = c("longitude", "latitude"), crs = 4326)
}
\arguments{
\item{x}{Occurrence records: an \code{sf} POINT object (for example the output of
\code{rl_occurrences()}), or a data frame holding longitude and latitude
columns.}

\item{coords}{Character vector of length two giving the longitude and
latitude column names when \code{x} is a data frame. Default
\code{c("longitude", "latitude")}.}

\item{crs}{Coordinate reference system of the input coordinates, passed to
\code{sf}. Used only when \code{x} is a data frame or an \code{sf} object without a CRS.
Default \code{4326} (WGS84).}
}
\value{
A one row \code{sf} object with the convex hull in its \code{geometry} column
(an empty polygon when fewer than three unique locations are available),
returned in the input coordinate system, and the columns:
\describe{
\item{\code{metric}}{\code{"EOO"}.}
\item{\code{area_km2}}{the extent of occurrence in square kilometres, or \code{NA}
when fewer than three unique locations are available.}
\item{\code{n_records}}{the number of records used.}
\item{\code{n_unique}}{the number of unique locations.}
\item{\code{method}}{the estimation method, \code{"convex hull"}.}
\item{\code{category_b1}}{the most threatened criterion B1 band the area reaches
(\code{"CR"}, \code{"EN"} or \code{"VU"}), or \code{NA} when it reaches none (the area exceeds
every band, or the metric is undefined). This is the spatial threshold
only, not a full assessment, which also requires the criterion B
subconditions.}
}
}
\description{
Compute the extent of occurrence from occurrence records as the area of the
minimum convex polygon (convex hull) that encloses all points, following the
IUCN Red List guidelines (section 4.9). The convex hull is the method the
guidelines recommend for assessing the spatial thresholds of criterion B1.
}
\details{
Geographic coordinates (longitude and latitude) are projected to a local
Lambert azimuthal equal area system before the area is measured, so the
result is returned in square kilometres regardless of the input coordinate
system.

The extent of occurrence is undefined with fewer than three unique locations,
since a polygon cannot be drawn; in that case \code{area_km2} is \code{NA} and a warning
is issued. The guidelines also state that when EOO is smaller than AOO it
should be raised to equal AOO; that adjustment is left to the assessor and is
not applied here.

\code{sf} is required and is asked for interactively when it is not installed.
}
\examples{
\dontrun{
occ <- data.frame(
  longitude = c(2.1, 2.6, 3.0, 2.4, 2.9),
  latitude = c(9.1, 9.5, 9.0, 9.8, 9.3)
)
rl_eoo(occ)
}
}
\references{
IUCN Standards and Petitions Committee. 2024. Guidelines for Using the IUCN
Red List Categories and Criteria. Version 16, section 4.9.
\url{https://www.iucnredlist.org/documents/RedListGuidelines.pdf}
}
\seealso{
\code{\link[=rl_aoo]{rl_aoo()}}
}
