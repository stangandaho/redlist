% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rl_occurrences.R
\name{rl_occurrences}
\alias{rl_occurrences}
\title{Retrieve GBIF occurrence records for a taxon}
\usage{
rl_occurrences(
  x,
  limit = 500,
  country = NULL,
  year = NULL,
  basis_of_record = NULL,
  has_coordinate = TRUE,
  has_geospatial_issue = FALSE,
  correct = NULL,
  progress = TRUE,
  crs = 4326,
  ...
)
}
\arguments{
\item{x}{The taxon to retrieve. One of:
\itemize{
\item a scientific name, for example \code{"Afzelia africana"};
\item a GBIF backbone taxon key (a number);
\item a data frame from a name resolution step (for example the output of
\code{\link[=rl_name_resolve]{rl_name_resolve()}} or \code{\link[=rl_scientific_name]{rl_scientific_name()}}), from which a name column
is detected.
}}

\item{limit}{Maximum number of records to return. Default \code{500}. Use \code{Inf}
to fetch every available record. The search API is paged in blocks of 300
behind the scenes, up to its ceiling of 100000 records; beyond that a GBIF
download (which needs an account) would be required.}

\item{country}{Optional ISO 3166-1 alpha-2 country code to restrict records,
for example \code{"BJ"} for Benin. Pass a vector for several countries (matched
as OR), for example \code{c("BJ", "NG")}.}

\item{year}{Optional year filter. A single year (\code{2000}), a \code{"min,max"} range
string (\code{"2000,2020"}, open-ended as \code{"2000,*"} or \code{"*,2000"}), or a
comparator string (\code{">2025"}, \code{">=2025"}, \code{"<2000"}, \code{"<=2000"}). A range is
one comma-separated string, not a vector.}

\item{basis_of_record}{Optional GBIF basis of record filter, for example
\code{"HUMAN_OBSERVATION"} or \code{"PRESERVED_SPECIMEN"}. Pass a vector for several
types (matched as OR), for example
\code{c("HUMAN_OBSERVATION", "MACHINE_OBSERVATION")}.}

\item{has_coordinate}{Logical. Keep only records that carry coordinates.
Default \code{TRUE}.}

\item{has_geospatial_issue}{Logical. Keep records that GBIF flags with a
geospatial issue. Default \code{FALSE} (drop flagged records).}

\item{correct}{Which readiness checks from \code{\link[=rl_check_occurrences]{rl_check_occurrences()}} to run
after the download finishes, and apply. One of:
\itemize{
\item \code{NULL} (default): run no checks.
\item \code{TRUE}: run every check and drop the records behind the clear-error
issues (\code{duplicates}, \code{outliers}, \code{country}, \code{ocean_points},
\code{centroids}).
\item a character vector of check names (for example
\code{c("outliers", "duplicates")}): run those and drop the records they flag.
}

\code{coordinate_precision} is always reported but never applied here, since
dropping imprecise but real records can gut the sample and bias the metrics;
to apply it deliberately, call \code{\link[=rl_check_occurrences]{rl_check_occurrences()}} with
\code{correct = "coordinate_precision"}. The report-only checks
(\code{unique_localities}, \code{institution_diversity}, \code{recency}) are reported but
not corrected.

When any correction removes records the cleaned \code{sf} is returned; otherwise
the readiness report is attached to the returned \code{sf} as its \code{"report"}
attribute. For finer control (thresholds, report without correcting), call
\code{\link[=rl_check_occurrences]{rl_check_occurrences()}} directly.}

\item{progress}{Logical. Show a progress bar while records are downloaded.
Default \code{TRUE}.}

\item{crs}{Coordinate reference system for the returned \code{sf} object. Default
\code{4326} (WGS84), the system GBIF coordinates use.}

\item{...}{Further named filters passed straight to \code{\link[rgbif:occ_search]{rgbif::occ_search()}},
for example \code{continent}, \code{institutionCode}, \code{elevation}, or
\code{coordinateUncertaintyInMeters}.}
}
\value{
An \code{sf} POINT object (WGS84 by default) with one row per occurrence
record and the GBIF fields returned by the search, such as \code{scientificName},
\code{eventDate}, \code{year}, \code{country}, \code{basisOfRecord}, \code{institutionCode}, and
\code{coordinateUncertaintyInMeters}. When no record matches, an empty \code{sf}
object is returned with a warning. Records with invalid coordinates
(missing, out of range, null island, or absence records) are always
dropped; further data quality checks run only when \code{correct} is set.
}
\description{
Fetch occurrence records from the Global Biodiversity Information Facility
(GBIF) and return them as a clean \code{sf} POINT object ready for the criterion B
metrics \code{\link[=rl_eoo]{rl_eoo()}} and \code{\link[=rl_aoo]{rl_aoo()}}. This uses the public GBIF search API
through \code{rgbif}, so no GBIF account, username, or password is needed. Only
bulk downloads (the GBIF download API) require credentials, and those are not
used here.
}
\details{
Because records are queried by the GBIF backbone taxon key, occurrences that
GBIF indexes under synonyms of the accepted name are already included.
}
\examples{
\dontrun{
# By scientific name, capped at 300 records, Benin only
occ <- rl_occurrences("Afzelia africana", limit = 300, country = "BJ")

# Pass any GBIF filter through `...`
occ <- rl_occurrences("Panthera leo", year = "2010,2020",
                      basis_of_record = "HUMAN_OBSERVATION")

# Download, then run and correct specific checks
occ <- rl_occurrences("Afzelia africana", limit = 500, country = "BJ",
                      correct = c("outliers", "duplicates"))

# Straight into a criterion B metric
rl_eoo(occ)
}
}
\seealso{
\code{\link[=rl_check_occurrences]{rl_check_occurrences()}}, \code{\link[=rl_eoo]{rl_eoo()}}, \code{\link[=rl_aoo]{rl_aoo()}}, \code{\link[=rl_name_resolve]{rl_name_resolve()}}
}
