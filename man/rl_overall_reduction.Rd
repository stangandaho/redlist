% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rl_reduction.R
\name{rl_overall_reduction}
\alias{rl_overall_reduction}
\title{Overall reduction across subpopulations for IUCN criterion A}
\usage{
rl_overall_reduction(
  past = NULL,
  present = NULL,
  reduction = NULL,
  subpopulation = NULL,
  subcriterion = c("A2", "A1", "A3", "A4")
)
}
\arguments{
\item{past}{Numeric vector of subpopulation sizes at the start of the window
(three generations ago).}

\item{present}{Numeric vector of subpopulation sizes at the end of the window.}

\item{reduction}{Numeric vector of subpopulation reductions (proportions).}

\item{subpopulation}{Optional labels for the subpopulations.}

\item{subcriterion}{Which criterion A subcriterion sets the thresholds for
\code{category_a}: \code{"A2"} (default), \code{"A1"}, \code{"A3"} or \code{"A4"}.}
}
\value{
A one row tibble with the overall \code{reduction} and \code{reduction_pct}, the
number of subpopulations, the summed past and present sizes, and
\code{category_a}. The per-subpopulation table is attached as the
\code{"subpopulations"} attribute.
}
\description{
Combine the reductions of several subpopulations into one reduction for the
taxon, following section 4.5.4 of the IUCN Red List guidelines. The overall
reduction is the change in the summed population, which equals the average of
the subpopulation reductions weighted by their size three generations ago.
}
\details{
Give any two of \code{past}, \code{present} and \code{reduction} for each subpopulation and
the third is worked out from \code{reduction = 1 - present / past}. The sizes
should already be projected to the start and end of the same window, for
example with \code{\link[=rl_reduction]{rl_reduction()}}.
}
\examples{
# Guidelines Example 1: past and present sizes for three subpopulations
rl_overall_reduction(
  past = c(10000, 8000, 12000),
  present = c(5000, 9000, 2000),
  subpopulation = c("Pacific", "Atlantic", "Indian")
)
}
\references{
IUCN Standards and Petitions Committee. 2024. Guidelines for Using the IUCN
Red List Categories and Criteria. Version 16, section 4.5.4.
}
\seealso{
\code{\link[=rl_reduction]{rl_reduction()}}
}
