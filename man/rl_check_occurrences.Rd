% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rl_check_occurrences.R
\name{rl_check_occurrences}
\alias{rl_check_occurrences}
\title{Assessment-readiness checks for occurrence data}
\usage{
rl_check_occurrences(
  x,
  coords = c("decimalLongitude", "decimalLatitude"),
  checks = NULL,
  correct = FALSE,
  recent_years = 20,
  precision_degrees = 2/111.32,
  terrestrial = TRUE,
  outlier_multiplier = 5
)
}
\arguments{
\item{x}{Occurrence records: an \code{sf} POINT object (for example the output of
\code{\link[=rl_occurrences]{rl_occurrences()}}) or a data frame with longitude and latitude columns.}

\item{coords}{Character vector of length two giving the longitude and latitude
column names when \code{x} is a data frame. Default
\code{c("decimalLongitude", "decimalLatitude")}.}

\item{checks}{Character vector selecting which checks to run. Default \code{NULL}
runs every check.}

\item{correct}{Which removable issues to fix by dropping the offending
records. \code{FALSE} (default) removes nothing and returns the report. \code{TRUE}
removes every clear-error check that was run (\code{duplicates}, \code{outliers},
\code{country}, \code{ocean_points}, \code{centroids}) but not \code{coordinate_precision},
since dropping imprecise but real records is a completeness trade-off; name
it explicitly to apply it. A character vector selects specific checks,
including \code{coordinate_precision}. The report-only checks
(\code{unique_localities}, \code{institution_diversity}, \code{recency}) describe the
dataset as a whole and cannot be corrected by removing records.}

\item{recent_years}{Number of years back from today within which at least one
record should fall. Default \code{20}.}

\item{precision_degrees}{Coordinate precision threshold in decimal degrees.
Records coarser than this (too few decimal places, or a stated uncertainty
larger than this distance) are flagged. The default, \code{2 / 111.32} (about
0.018 degrees), corresponds to the 2 km AOO reference cell, so a record is
flagged only when it genuinely cannot be placed in a 2 km grid cell.}

\item{terrestrial}{Logical. Treat the taxon as terrestrial and check for
records in the ocean. Default \code{TRUE}.}

\item{outlier_multiplier}{Sensitivity of the outlier check: the multiplier
passed to \code{\link[CoordinateCleaner:cc_outl]{CoordinateCleaner::cc_outl()}}. Smaller flags more points. Default
\code{5}.}
}
\value{
When \code{correct = FALSE}, a tibble with one row per check and the
columns \code{check}, \code{status} (\code{"pass"}, \code{"warn"}, \code{"fail"} or \code{"skip"}),
\code{n_flagged} and \code{detail}, returned invisibly after the results are printed.
When \code{correct} removes issues, the cleaned occurrences are returned instead
(same class as \code{x}), with the report attached as the \code{"report"} attribute.
}
\description{
Run a set of data quality checks on occurrence records before they are passed
to the criterion B metrics \code{\link[=rl_eoo]{rl_eoo()}} and \code{\link[=rl_aoo]{rl_aoo()}}. The checks flag issues
that would compromise or bias the metrics. By default nothing is removed; the
function reports what it finds so the assessor can decide how to proceed. Set
\code{correct} to also drop the records behind the removable issues and return the
cleaned data, ready to pass straight to \code{\link[=rl_eoo]{rl_eoo()}} or \code{\link[=rl_aoo]{rl_aoo()}}.
}
\details{
The available checks are:
\describe{
\item{\code{unique_localities}}{fewer than 3 unique localities (EOO is undefined
below 3 points). Report only.}
\item{\code{institution_diversity}}{all records from a single institution
(possible collection bias). Report only.}
\item{\code{recency}}{no records within the recency window (the data may be
stale). Report only.}
\item{\code{duplicates}}{records sharing the same coordinate, event date, and
institution. Removable.}
\item{\code{coordinate_precision}}{coordinates coarser than a threshold, from few
decimal places or a large stated uncertainty (too imprecise for the 2 by
2 km AOO grid). Removable.}
\item{\code{outliers}}{spatial outliers far from the main cluster, which inflate
the EOO convex hull. Removable. Needs \code{CoordinateCleaner}.}
\item{\code{country}}{coordinates that fall outside the record's stated country
(sign or transposition errors). Removable. Needs \code{CoordinateCleaner} and a
\code{countryCode} column.}
\item{\code{ocean_points}}{records in the ocean for a terrestrial taxon.
Removable. Needs \code{CoordinateCleaner}.}
\item{\code{centroids}}{country and capital centroids, biodiversity-institution
and GBIF headquarters coordinates, and plain zeros. Removable. Needs
\code{CoordinateCleaner}.}
}
Checks needing \code{CoordinateCleaner} are skipped, with a note, when it is not
installed.
}
\examples{
\dontrun{
occ <- rl_occurrences("Afzelia africana", limit = 500, country = "BJ")

# Report only
rl_check_occurrences(occ)

# Clean and feed straight into a metric
clean_occ <- rl_check_occurrences(occ, correct = TRUE)
rl_aoo(clean_occ)
}
}
\seealso{
\code{\link[=rl_occurrences]{rl_occurrences()}}, \code{\link[=rl_eoo]{rl_eoo()}}, \code{\link[=rl_aoo]{rl_aoo()}}
}
