% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rl_name_resolve.R
\name{rl_name_resolve}
\alias{rl_name_resolve}
\title{Resolve a scientific name to the accepted IUCN Red List name}
\usage{
rl_name_resolve(
  genus_name,
  species_name,
  infra_name = NULL,
  subpopulation_name = NULL,
  vernaculars = "all",
  data_sources = 163,
  all_matches = FALSE,
  capitalize = FALSE,
  species_group = FALSE,
  fuzzy_uninomial = FALSE,
  stats = FALSE,
  main_taxon_threshold = 0.6
)
}
\arguments{
\item{genus_name}{Character. The genus name (required).}

\item{species_name}{Character. The species name (required).}

\item{infra_name}{Character. The infraspecific name (optional).}

\item{subpopulation_name}{Character. The subpopulation name (optional).}

\item{vernaculars}{Character. Vernacular-name languages to return, as
\code{"|"}-separated ISO 639-3 codes (e.g. "eng|rus|deu" for multiple languages),
or \code{"all"} for every language found. Default \code{"all"}.}

\item{data_sources}{Integer vector of GlobalNames data-source ids to match
against. Default \code{163} for IUCN.  See
\url{https://verifier.globalnames.org/data_sources} for the full list.}

\item{all_matches}{Logical. If \code{TRUE}, return every match found instead of
only the best one. Default \code{FALSE}.}

\item{capitalize}{Logical. Capitalize the first letter of the name before
matching. Default \code{FALSE}.}

\item{species_group}{Logical. Expand the search to the species group where
applicable. Default \code{FALSE}.}

\item{fuzzy_uninomial}{Logical. Allow fuzzy matching for uninomial names.
Default \code{FALSE}.}

\item{stats}{Logical. Ask the API to find the kingdom and main taxon holding
most names (Catalogue of Life only). Default \code{FALSE}.}

\item{main_taxon_threshold}{Numeric between 0.5 and 1 setting the minimal
proportion for main-taxon discovery. Default \code{0.6}.}
}
\value{
A tibble with one row per input name. A name that cannot be
matched returns a single row of \code{NA} fields.
}
\description{
When you work with names from another source (for example GBIF), some names
differ from the ones the IUCN Red List uses, so a direct request such as
\code{\link[=rl_scientific_name]{rl_scientific_name()}} returns \emph{404 (species not found)}. This function forces
a match: it verifies the name against the
\href{https://verifier.globalnames.org/apidoc}{GlobalNames Verifier} API and
returns the accepted (current) name the IUCN Red List uses, so the retrieval
can be repeated with the resolved name.
}
\examples{
\dontrun{
# GBIF's "Corvinella corvina" is a synonym; IUCN uses "Lanius corvinus"
rl_name_resolve(genus_name = "Corvinella", species_name = "corvina")
}
}
\seealso{
\code{\link[=rl_scientific_name]{rl_scientific_name()}}
}
